{
  "name": "tovo",
  "version": "0.1.0",
  "private": true,
  "description": "Tovo — application de livraison conversationnelle (Niamey, Niger)",
  "scripts": {
    "db:link": "supabase link --project-ref",
    "db:push": "supabase db push",
    "db:diff": "supabase db diff",
    "db:reset": "supabase db reset --linked"
  },
  "devDependencies": {
    "supabase": "^2.111.0"
  }
}
